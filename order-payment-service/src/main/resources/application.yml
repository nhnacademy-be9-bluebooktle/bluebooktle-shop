spring:
  application:
    name: order-payment-service
  jpa:
    open-in-view: false

# Eureka 클라이언트 기본 설정
#eureka:
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#  instance:
#    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
#    prefer-ip-address: true


logging:
  level:
    shop.bluebooktle.orderpayment: INFO