spring:
  application:
    name: eureka-server

# Eureka 서버 설정: 자신은 등록/조회하지 않음
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    wait-time-in-ms-when-sync-empty: 0 # 단일 서버 운영 시 설정 (클러스터 시 변경)

logging:
  level:
    shop.bluebooktle.eureka: INFO # 기본 로깅 레벨