server:
  port: 10214

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

#eureka:
#  client:
#    serviceUrl:
#      defaultZone: ${EUREKA_URL}

logging:
  level:
    shop.bluebooktle.coupon: INFO